// Generated by CoffeeScript 1.6.3
(function() {
  'use strict';
  var guid;

  window.DataCon || (window.DataCon = {});

  guid = window.location.href.split('/')[window.location.href.split('/').length - 1];

  $(function() {
    return $.getJSON("/api/getdata/" + guid, function(data) {
      var key, _fn;
      DataCon.dataset = data;
      DataCon.keys = [];
      if (data.length) {
        DataCon.patterns = {};
        _fn = function(key) {
          DataCon.keys.push(key);
          return DataCon.patterns[key] = getPattern(data[0][key]);
        };
        for (key in data[0]) {
          _fn(key);
        }
        return new PGOverviewCharts(DataCon, '#charts');
      }
    });
  });

}).call(this);
